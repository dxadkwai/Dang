import java.util.Arrays;

public class method {
    public static void main(String[] args) {
        // int[] nums = {3,5,8,10,9,26,4,1};
        // int[] backups=nums;
        // // String[] names = {"Frank", "Tank", "Clank", "bruh"};
        // // System.out.println(Arrays.toString(nums));
        // // Arrays.sort(nums);
        // // System.out.println(Arrays.toString(nums));
        // // System.out.println(Arrays.toString(names));
        // // Arrays.sort(names);
        // // System.out.println(Arrays.toString(names));

        // Arrays.sort(nums);
        // int index = Arrays.binarySearch(nums, 6);
        // System.out.println("Index of 6 is at" + index);

        int [] nums = new int[5];
        Arrays.fill(nums, 0,3,10);//array,fromindex,toindex,value.
        System.out.println(Arrays.toString(nums));


    }
    
}
